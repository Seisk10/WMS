PORT=3001
NODE_ENV=development

# Gere um valor forte em produção: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=troque_por_um_segredo_forte_antes_de_produzir
JWT_EXPIRES_IN=8h
